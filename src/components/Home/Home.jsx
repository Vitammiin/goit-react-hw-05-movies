import React from 'react';
import { Routes, Route, NavLink } from 'react-router-dom';

const Home = () => {
  return (
    <div>
      <div>
        <p>IT Home</p>
        <ul>{}</ul>
      </div>
    </div>
  );
};

export default Home;

//className={isActiv => `${css.headbut}`}
